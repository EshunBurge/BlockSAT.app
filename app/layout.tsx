import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BlockSAT — Study. Stack. Score.",
  description: "Answer SAT-style questions to unlock puzzle pieces in an original block-stacking game. Studying becomes the progression mechanic.",
};

// `viewportFit: "cover"` lets the app draw edge-to-edge on notched/rounded
// iOS devices; the `.pt-safe` / `.pb-safe` utilities (globals.css) then use
// `env(safe-area-inset-*)` to keep content clear of the notch and home
// indicator when this is wrapped as a native app.
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  viewportFit: "cover",
  themeColor: "#0a0a0a",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
